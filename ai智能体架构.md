# MCP Sidecar 架构文档

## 1. 架构概述

### 1.1 核心定位
MCP Sidecar是一个轻量级的中间件代理服务，主要承担两个核心职能：
- **MCP服务网关**: 作为MCP（Model Context Protocol）服务的统一入口，支持接入外部系统作为MCP服务源
- **AI Agent代理**: 转发Python AI Agent的WebSocket接口，在中间加入鉴权和限流等安全控制逻辑

### 1.2 系统组成
整个系统由两个核心组件构成：
- **Agentic App**: 基于LangChain的智能代理系统，实现多轮、交互式、自我优化的复杂任务处理
- **MCP Sidecar**: 作为代理网关，提供安全接入和服务治理能力

### 1.3 架构目标
- **统一接入**: 为MCP服务和AI Agent提供统一的接入点
- **安全控制**: 实现身份鉴权和流量管理
- **协议转换**: 支持不同协议间的消息转换和路由
- **服务治理**: 提供监控、日志和服务管理能力

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
├─────────────────────────────────────────────────────────────┤
│  MCP客户端  │  AI Agent  │  第三方系统  │  管理端           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    MCP Sidecar 网关层                        │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                   代理服务层                              │ │
│  │ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────┐ │ │
│  │ │   MCP服务路由    │ │   AI Agent代理   │ │   统一管理   │ │ │
│  │ │                 │ │                 │ │             │ │ │
│  │ │ • 协议转换      │ │ • WS连接管理    │ │ • 配置管理   │ │ │
│  │ │ • 服务路由      │ │ • 消息转发      │ │ • 健康检查   │ │ │
│  │ │ • 错误处理      │ │ • 心跳保持      │ │ • 服务监控   │ │ │
│  │ │ • 缓存管理      │ │ • 状态同步      │ │ • 日志管理   │ │ │
│  │ └─────────────────┘ └─────────────────┘ └─────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                              │                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                   安全控制层                              │ │
│  │ ┌─────────────────┐ ┌─────────────────┐                │ │
│  │ │   身份鉴权      │ │   连接限流      │                │ │
│  │ │                 │ │                 │                │ │
│  │ │ • Token验证     │ │ • 连接数限制    │                │ │
│  │ │ • 认证中心对接  │ │ • 用户级限流    │                │ │
│  │ │ • 会话管理      │ │ • IP级限流      │                │ │
│  │ └─────────────────┘ └─────────────────┘                │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        后端服务层                            │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                  MCP服务源                                │ │
│  │ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────┐ │ │
│  │ │   外部系统A     │ │   外部系统B     │ │   内部服务   │ │ │
│  │ │                 │ │                 │ │             │ │ │
│  │ │ • 工具服务      │ │ • 数据服务      │ │ • 业务服务   │ │ │
│  │ │ • 计算服务      │ │ • 存储服务      │ │ • 管理服务   │ │ │
│  │ └─────────────────┘ └─────────────────┘ └─────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                Agentic App 智能代理                     │ │
│  │ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────┐ │ │
│  │ │   意图识别      │ │   工具执行      │ │   记忆管理   │ │ │
│  │ │                 │ │                 │ │             │ │ │
│  │ │ • 意图分析      │ │ • RAG检索      │ │ • 会话持久   │ │ │
│  │ │ • 参数补全      │ │ • 动态工具      │ │ • 上下文管理 │ │ │
│  │ │ • 工具选择      │ │ • MCP集成      │ │ • 多用户支持 │ │ │
│  │ └─────────────────┘ └─────────────────┘ └─────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 3. 核心组件架构

### 3.1 Agentic App 智能代理系统

#### 3.1.1 系统概述
Agentic App是一个基于LangChain的智能代理系统，实现多轮、交互式、自我优化的复杂任务处理。通过反应式工作流能够理解用户意图，动态补充参数，调用工具完成查询。

#### 3.1.2 核心能力
- **意图识别与参数补全**: 自动分析用户意图，动态请求缺失参数
- **反应式工作流**: 基于LangGraph的状态机，支持评估迭代优化
- **动态工具系统**: 配置驱动的工具生成，支持RAG知识库检索
- **MCP多服务器集成**: 支持连接多个MCP服务器，动态加载外部工具
- **记忆持久化**: SQLite/JSON双重存储，多用户对话管理
- **实时交互**: WebSocket流式API，支持工具调用状态可视化

#### 3.1.3 工作流架构
```
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│  意图识别   │ → │  参数补全   │ → │  工具执行   │
└─────────────┘   └─────────────┘   └─────────────┘
        ↑                   ↓                 ↓
        │              ┌─────────────┐   ┌─────────────┐
        └──────────────│ 评估迭代    │ ← │  结果评估   │
                       └─────────────┘   └─────────────┘
                                ↓
                        ┌─────────────┐
                        │  最终答案   │
                        └─────────────┘
```

### 3.2 代理服务层

#### 3.2.1 MCP服务路由
**功能描述**:
- 作为MCP服务的统一入口，接收和处理MCP协议请求
- 支持多个外部MCP服务源的接入和管理

**核心职责**:
- **协议解析**: 解析MCP协议消息，验证消息格式
- **服务路由**: 根据请求类型路由到对应的外部MCP服务
- **错误处理**: 统一处理MCP服务调用异常
- **缓存管理**: 缓存MCP服务响应，提高性能

#### 3.2.2 AI Agent代理
**功能描述**:
- 转发Agentic App的WebSocket连接和消息
- 在代理层注入安全控制逻辑
- 提供连接管理和状态监控

**核心职责**:
- **连接管理**: 管理与Agentic App的WebSocket连接生命周期
- **消息转发**: 双向转发客户端与Agentic App之间的消息
- **心跳保持**: 维持连接活性，自动重连机制
- **状态同步**: 同步连接状态和会话信息
- **性能监控**: 监控连接质量和消息处理性能

**与Agentic App的集成**:
- **协议转换**: 将客户端协议转换为Agentic App的WebSocket协议
- **消息路由**: 根据用户ID和会话ID路由到对应的Agentic App实例
- **状态管理**: 维护用户会话状态和连接状态
- **工具调用状态**: 传递工具调用状态信息给客户端

#### 3.2.3 统一管理
**功能描述**:
- 提供配置管理和运维能力
- 集中管理代理服务的各项配置
- 提供健康检查和服务监控

**核心职责**:
- **配置管理**: 集中管理服务配置、路由规则、安全策略
- **健康检查**: 定期检查服务健康状态
- **服务监控**: 收集服务运行指标，提供监控视图
- **日志管理**: 集中收集和管理服务日志
- **告警通知**: 基于监控指标自动触发告警

### 3.3 安全控制层

#### 3.3.1 身份鉴权
**功能描述**:
- 对接认证中心，实现统一的身份鉴权
- 支持Token验证和会话管理

**核心职责**:
- **Token验证**: 验证客户端请求的访问Token有效性
- **会话管理**: 管理用户会话状态和生命周期
- **认证中心对接**: 与认证中心API集成，获取用户信息
- **异常处理**: 处理鉴权失败、Token过期等异常情况

**鉴权流程**:
```
客户端请求 → Token提取 → 认证中心验证 → 用户信息获取 → 会话创建 → 请求放行
```

#### 3.3.2 连接限流
**功能描述**:
- 实现基于连接数量的限流控制
- 防止系统过载和恶意攻击

**核心职责**:
- **连接数限制**: 限制同时连接的数量
- **用户级限流**: 基于用户的连接数限制
- **IP级限流**: 基于IP地址的连接数限制

## 4. 数据流架构

### 4.1 MCP服务调用流程
```
客户端请求 → MCP路由 → 协议解析 → 服务路由 → MCP服务调用 → 结果缓存 → 响应返回
```

### 4.2 AI Agent代理流程
```
客户端连接 → 身份鉴权 → 连接限流 → 连接建立 → 消息转发 → 心跳保持 → 连接管理
```

### 4.3 Agentic App工作流
```
用户查询 → 意图识别 → 参数补全 → 工具执行 → 结果评估 → 最终答案
```

### 4.4 鉴权流程
```
用户请求 → Token提取 → 认证中心验证 → 用户信息获取 → 会话创建 → 请求放行
```

## 5. 部署架构

### 5.1 部署模式
```
┌─────────────────────────────────────────────────────────────┐
│                        部署架构                              │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                  MCP Sidecar 实例                        │ │
│  │ ┌─────────────────────────────────────────────────────┐ │ │
│  │ │              AI Agent代理服务                        │ │ │
│  │ │ • WS连接管理  • 消息转发  • 心跳保持                │ │ │
│  │ └─────────────────────────────────────────────────────┘ │ │
│  │ ┌─────────────────────────────────────────────────────┐ │ │
│  │ │              MCP服务路由                             │ │ │
│  │ │ • 协议转换  • 服务路由  • 错误处理                  │ │ │
│  │ └─────────────────────────────────────────────────────┘ │ │
│  │ ┌─────────────────────────────────────────────────────┐ │ │
│  │ │              安全控制层                              │ │ │
│  │ │ • 身份鉴权  • 连接限流                              │ │ │
│  │ └─────────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                              │                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                  Agentic App 实例                       │ │
│  │ ┌─────────────────────────────────────────────────────┐ │ │
│  │ │              智能代理系统                             │ │ │
│  │ │ • 意图识别  • 参数补全  • 工具执行  • 记忆管理      │ │ │
│  │ └─────────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                              │                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                  数据存储                                │ │
│  │            ┌─────────────┐ ┌─────────────┐              │ │
│  │            |    Log      │ │   Monitor   │              │ │
│  │            │   Storage   │ │   Storage   │              │ │
│  │            └─────────────┘ └─────────────┘              │ │
│  └─────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────── ┘
```

## 6. 总结

MCP Sidecar结合Agentic App智能代理系统，构建了一个完整的智能代理服务架构：

**核心优势**:
1. **统一接入**: 提供统一的入口点，简化客户端接入复杂度
2. **智能代理**: 集成Agentic App的意图识别、参数补全、工具调用等智能能力
3. **安全可控**: 身份鉴权和连接限流机制
4. **高性能**: 缓存管理和连接优化，提供高性能服务
5. **易扩展**: 插件化架构，支持快速功能扩展

**技术特色**:
- **LangGraph工作流**: 基于LangGraph的智能工作流引擎
- **多工具集成**: RAG、动态工具、MCP工具的统一管理
- **实时交互**: WebSocket流式API，支持工具调用状态可视化
- **记忆持久化**: 多用户会话持久化和上下文管理
- **协议转换**: 支持多种协议间的转换和路由

## 7. 待优化功能
1. 调用mcp服务时，通过用户标识来确定用户是否有对应接口数据的查看权限
2. 大模型对话调用日志记录
3. 大模型mcp调用第三方系统API日志记录
4. 分布式限流